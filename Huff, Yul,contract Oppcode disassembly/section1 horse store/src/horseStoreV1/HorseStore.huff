// find function selector -> route it to the code that updates horses
// PUSH0 on to the stack to do smome stuff later
// we can use PUSH0 or 0x00 ,PUSH1 or 0x01 ,PUSH2 or 0x0123 as its 2bits long,huff will be able to understand both
#define macro MAIN()=takes(0) returns(0){
    0x00   //[0]
    // 0x02   //TOP [2,0] BOTTOM
    calldataload //[calldata]
    // cut down calldata -> function selector
    // SHR opcode
    // 0x0102 (bytes)
    // 1 bytes = 8 bits
}